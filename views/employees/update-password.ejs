<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Parolni o'zgartirish</title>
  <style>
    /* Avvalgi sahifalardagi CSS uslublarining asosi (body, .container, input, button, error-list, success-msg) */
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(to right, #f1c40f, #f39c12);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      margin: 0;
    }

    .container {
      background: #fff;
      padding: 30px 40px;
      border-radius: 12px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.2);
      width: 400px;
    }

    h1 {
      text-align: center;
      margin-bottom: 25px;
      color: #333;
    }

    .form-group {
        margin-bottom: 15px;
    }

    label {
        display: block;
        margin-bottom: 5px;
        font-weight: 600;
        color: #555;
    }

    input[type="password"] {
      width: 100%;
      padding: 10px 12px;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 14px;
      box-sizing: border-box;
    }

    button {
      width: 100%;
      padding: 12px;
      background: #f39c12;
      color: white;
      font-size: 16px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: 0.3s;
      margin-top: 10px;
    }

    button:hover {
      background: #e67e22;
    }

    .error-list {
      list-style: none;
      padding: 0;
      margin-bottom: 15px;
    }

    .error-list li {
      color: #e74c3c;
      font-size: 14px;
      margin-bottom: 5px;
    }

    .success-msg {
      color: #27ae60;
      font-size: 15px;
      margin-bottom: 15px;
      text-align: center;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Parolni o'zgartirish 🔒</h1>

    <% if (errors && errors.length > 0) { %>
      <ul class="error-list">
        <% errors.forEach(err => { %>
          <li><%= err.message %></li>
        <% }) %>
      </ul>
    <% } %>

    <% if (success) { %>
      <p class="success-msg"><%= success %></p>
    <% } %>
    

    <form action="/employees/update-password" method="POST">
      
      <div class="form-group">
        <label for="oldPassword">Joriy parol:</label>
        <input type="password" name="oldPassword" required />
      </div>

      <div class="form-group">
        <label for="newPassword">Yangi parol:</label>
        <input type="password" name="newPassword" required />
      </div>
      
      <div class="form-group">
        <label for="confirmNewPassword">Yangi parolni tasdiqlash:</label>
        <input type="password" name="confirmNewPassword" required />
      </div>

      <button type="submit">Parolni o'zgartirish</button>
    </form>
    
    <p style="text-align: center; margin-top: 15px;">
        <a href="/employees/profile" style="color: #f39c12; text-decoration: none;">Profilga qaytish</a>
    </p>
  </div>
</body>
</html>